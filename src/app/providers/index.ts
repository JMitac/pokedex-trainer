/**
 * @file index.ts
 * @layer App / Providers
 *
 * Punto de entrada único de los providers de la aplicación.
 *
 * Uso:
 *   import { QueryProvider } from '@/app/providers';
 */

export { QueryProvider, queryClient } from './QueryProvider';
