/**
 * @file index.ts
 * @layer UI / Components / Button
 *
 * Punto de entrada único del componente Button.
 *
 * Uso:
 *   import { Button } from '@/ui/components/Button';
 */

export { Button } from './Button';
export type { ButtonProps, ButtonVariant, ButtonSize } from './Button';
