/**
 * @file index.ts
 * @layer UI / Components / Input
 *
 * Punto de entrada único del componente Input.
 *
 * Uso:
 *   import { Input } from '@/ui/components/Input';
 */

export { Input } from './Input';
export type { InputProps, InputVariant } from './Input';
