/**
 * @file index.ts
 * @layer Shared / API
 *
 * Punto de entrada único del módulo de API.
 *
 * Uso:
 *   import { httpClient } from '@/shared/api';
 */

export { httpClient } from './httpClient';
